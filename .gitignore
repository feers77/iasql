# Secrets — never commit
.env

# Deployment-specific (contains this host's tailscale IP)
viewer/*.live.service
__pycache__/

# Generated static site (published to the gh-pages branch)
site/

# Build artifacts
*.o
*.so
*.bc
*.bin
build/
results/
*.log
